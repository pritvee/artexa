import { chromium } from 'playwright'; import fs from 'fs'; (async () => { const browser = await chromium.launch(); const page = await browser.newPage(); const logs = []; page.on('console', msg => logs.push('CONSOLE: ' + msg.text())); page.on('pageerror', err => logs.push('PAGEERROR: ' + err.message + '\n' + err.stack)); await page.goto('http://localhost:5173/customize/giftbox/3'); await page.waitForTimeout(3000); fs.writeFileSync('logs.txt', logs.join('\n')); await browser.close(); })();
